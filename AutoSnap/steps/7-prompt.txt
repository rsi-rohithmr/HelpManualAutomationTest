
=====================
Full Prompt for Step 7
=====================

-------- System Instruction --------
You are a world-class Playwright code-generation expert, relentlessly focused on SPEED and ROBUSTNESS. Your primary job is to generate the most direct and successful Playwright code for the next action, ensuring it executes quickly and reliably. Never use text-based locators for screenshots; prefer stable attributes like data-testid/role/label and container elements.

-------- Scenario Text --------
```json
{
  "thinking": "Screenshots to capture: 1. img_as/documentviewer_S.png: Shows the Document Viewer interface with the document list in the left panel and the preview area in the right panel. 2. img_as/documentexplorer_E.png: Displays the Document Explorer functionality and layout specifically from the left panel. 3. img_as/patientdetails_E.png: Captures the patient details section including demographic details and identifiers. 4. ./img/popuppatientinfo.png: Highlights the pop-out patient details interface with patient demographics. 5. ./img/bmistatus.png: Displays BMI measurement with tooltip indicators. 6. ./img/patientcards.png: Shows the patient card interface including vital signs and demographic details. 7. ./img/filterdocument.png: Captures the filter document interface where user can filter among document types. 8. ./img/uploaddocument.png: Displays the upload document widget with options to upload files. 9. ./img/addnotes.png: Captures the interface for adding notes. 10. ./img/editdeletenotes.png: Displays the interface for editing and deleting notes. 11. ./img/alert.png: Shows the alert message for creating additional reports. 12. ./img/LPR1.png: Captures the initial view of Linked Patient Records in the Document Viewer. 13. ./img/LPR2.png: Displays prior studies of linked patients within the viewer. 14. ./img/LPR3.png: Shows a detailed view of prior studies including associated reports. 15. ./img/LPR4.png: Captures the viewport displaying linked patient information. 16. ./img/LPR5.png: Highlights the linked patient name in the viewer interface. 17. ./img/alert2.png: Displays the alert for editing lock status. 18. ./img/RAI1.png: Captures the interface for starting the editing of a report. 19. ./img/RAI2New.png: Displays the automatic lock functionality while editing a report. 20. ./img/RAI3.png: Shows the editing indicator with the active editor's name. 21. ./img/RAI4.png: Captures the automatic unlock functionality. 22. ./img/RAI5.png: Displays the button to take over editing after inactivity. Navigation plan: Homepage (Lista de Trabalho) → Wheel Interface → Document Viewer (capture img_as/documentviewer_S.png and img_as/documentexplorer_E.png) → Patient Details Section (capture img_as/patientdetails_E.png) → Pop-out patient details interface (capture ./img/popuppatientinfo.png) → BMI tooltip and vital signs (capture ./img/bmistatus.png and ./img/patientcards.png) → Document Management Section (capture ./img/filterdocument.png and ./img/uploaddocument.png) → Notes Interface (capture ./img/addnotes.png and ./img/editdeletenotes.png) → Alert Messages Interface (capture ./img/alert.png and ./img/alert2.png) → Linked Patient Records Section (capture ./img/LPR1.png to ./img/LPR5.png) → Report Editing System (capture ./img/RAI1.png to ./img/RAI5.png). UI State Requirements: Ensure that all dropdowns, tooltips, and hover features are expanded where applicable. Verify all relevant UI elements are visible and fully loaded. Target Language Element Mapping: Document Viewer = 'Visualizador de Documentos', Worklist = 'Lista de Trabalho', Patient Details = 'Detalhes do Paciente', BMI = 'IMC', Vital Signs = 'Sinais Vitais', Filter Document = 'Filtrar Documento', Upload Document = 'Enviar Documento', Notes = 'Notas', Linked Patient Records = 'Registros de Pacientes Vinculados', Report Editing = 'Edição de Relatórios'. Sequence Optimization: Group screenshots for the same interface together (e.g., Document Viewer screenshots captured consecutively).",
  "instructions": "1. Locate the worklist table ('Lista de Trabalho') displayed in the center of the homepage showing patient records. Find the third patient record row in the worklist table and click on the patient name link ('Nome do Paciente') to open the wheel interface. 2. Wait until the wheel interface loads completely with all available options visible around the circular menu. 3. Locate the 'Visualizador de Documentos' button (Document Viewer in target language) on the wheel interface — it appears as a paper/document icon in the wheel menu — and click on it. 4. Wait until the Document Viewer interface loads showing the document list panel on the left and preview area on the right. 5. Take a screenshot of the complete Document Viewer interface showing the left panel with the document list and the right panel with the document preview area, to document the document viewing functionality. Save as img_as/documentviewer_S.png. 6. Locate the left panel within the Document Viewer — labeled as 'Explorador de Documentos' in the target language — and ensure it is fully visible. 7. Take a screenshot of the left panel showing the Document Explorer functionality and layout. Save as img_as/documentexplorer_E.png. 8. Locate the top section of the Document Explorer — labeled 'Detalhes do Paciente' in the target language — and ensure patient details are fully visible. 9. Take a screenshot of the patient details section including identifiers and demographic details. Save as img_as/patientdetails_E.png. 10. Locate the pop-out icon ('Abrir detalhes do paciente') in the patient details section and click on it to open the patient details in a new tab. 11. Wait until the pop-out patient details interface fully loads. 12. Take a screenshot of the pop-out patient details interface showing all demographic details. Save as ./img/popuppatientinfo.png. 13. Locate the BMI tooltip ('IMC') in the patient details section and hover over it to reveal the BMI status indicator. 14. Take a screenshot of the BMI measurement displayed with the tooltip indicator. Save as ./img/bmistatus.png. 15. Scroll down within the patient details section to locate the vital signs ('Sinais Vitais') displayed below the BMI measurement. 16. Take a screenshot of the vital signs section including height, weight, heart rate, and blood pressure. Save as ./img/patientcards.png. 17. Navigate to the document management section within the Document Viewer labeled 'Filtrar Documento' and 'Enviar Documento' in the target language. 18. Ensure the filter document dropdown is expanded to show all document types. 19. Take a screenshot of the filter document interface showing all options. Save as ./img/filterdocument.png. 20. Locate the upload icon ('Enviar Documento') within the document management section and click on it to open the upload widget. 21. Wait until the upload widget fully loads. 22. Take a screenshot of the upload document interface showing all upload options. Save as ./img/uploaddocument.png. 23. Locate the notes section labeled 'Notas' in the target language within the Document Viewer. 24. Click on the 'Add Notes' button ('Adicionar Notas') to open the add notes interface. 25. Wait until the add notes interface fully loads. 26. Take a screenshot of the add notes interface showing all fields and options. Save as ./img/addnotes.png. 27. Locate an existing note in the notes section and hover over it to reveal the edit and delete options ('Editar' and 'Excluir'). 28. Take a screenshot of the edit and delete notes interface showing all available options. Save as ./img/editdeletenotes.png. 29. Navigate to the alert messages section within the Document Viewer. 30. Locate the alert interface for creating additional reports labeled 'Relatório já existe' in the target language. 31. Take a screenshot of the alert message showing the options 'Cancelar' and 'Continuar'. Save as ./img/alert.png. 32. Navigate to the Linked Patient Records section within the Document Viewer. 33. Ensure the initial view of Linked Patient Records labeled 'Registros de Pacientes Vinculados' is fully visible. 34. Take a screenshot of the initial Linked Patient Records interface. Save as ./img/LPR1.png. 35. Click on the 'Estudos Anteriores' category to expand and display prior studies. 36. Wait until prior studies load completely. 37. Take a screenshot of the prior studies interface showing all associated reports. Save as ./img/LPR2.png. 38. Locate a specific prior study within the list and click on it to view the detailed report. 39. Wait until the report fully loads in the viewport. 40. Take a screenshot of the viewport displaying linked patient information and the associated report. Save as ./img/LPR3.png. 41. Switch to another linked patient record within the Linked Patient Records section. 42. Take a screenshot of the viewport showing the new linked patient record. Save as ./img/LPR4.png. 43. Locate the linked patient name ('Nome do Paciente Vinculado') displayed in the viewport for identification. 44. Take a screenshot of the linked patient name within the viewer interface. Save as ./img/LPR5.png. 45. Navigate to the Report Editing section within the Document Viewer. 46. Locate the interface for starting the editing process labeled 'Edição de Relatórios' and click on the 'Editar Relatório' button. 47. Wait until the editing interface fully loads. 48. Take a screenshot of the initial report editing interface. Save as ./img/RAI1.png. 49. Begin editing the report and observe the automatic lock functionality labeled 'Bloqueio Automático'. 50. Take a screenshot of the automatic lock interface showing the lock symbol and editing tools. Save as ./img/RAI2New.png. 51. Observe the editing indicator labeled 'Você está editando' displaying the active editor's name. 52. Take a screenshot of the editing indicator interface. Save as ./img/RAI3.png. 53. Leave the report inactive for 15 minutes and observe the automatic unlock functionality. 54. Take a screenshot of the automatic unlock interface showing the unlocked status. Save as ./img/RAI4.png. 55. Locate the 'Assumir Edição' button within the unlocked report interface and click on it to take over editing. 56. Take a screenshot of the take over editing interface showing all available options. Save as ./img/RAI5.png. Based on the instructions executed, if you think some screenshot taken is not right, redo the process to get that screenshot."
}
```

-------- Code Generation Prompt --------
You are an expert Playwright AI assistant. Analyze the provided context and user script to generate a small, efficient snippet of Playwright TypeScript code (1-2 lines) for the very next action.

  ### Context Inputs You Will Receive:
  - **Current Page URL**: The URL of the page being automated.
  - **Current Page Screenshot**: An image of the current viewport.
  - **Current Page Visible HTML**: A list of indexed, interactive elements (e.g., 42: <button...>Login</button>). Each element may include appended state information.
  - **Already Executed Code**: The history of code successfully run in the current session.
  - **Previous Step Error**: An error message if the last attempt failed.
  - **User Script**: The complete, ordered list of steps the user wants to automate.
  
  ### Required Output Format:
  JSON with "thinking", "code", and "screenshotIntent" (screenshots only)
  
  **Thinking:** Analyze instruction, check element states, choose action
  **Code:** 1-2 lines starting with await. If done: "done"
  
  ### Core Element Selection Rules:
  
  **1. Ground Your Element Choice in Visible HTML:**
  - You MUST base your element selection exclusively on the elements provided in the Visible HTML. Never invent elements.
  
  **2. Strict Locator Hierarchy (Choose the Best Available):**
    1. page.getByTestId('stable-test-id')
    2. page.getByRole('button', { name: 'Submit' })
    3. page.getByLabel('Username')
    4. page.getByText('Welcome, User!', { exact: true })
  
  **3. Ensure Uniqueness:**
  - Your locator MUST resolve to a single element.
  - DO NOT USE .first(), .last(), or index-based selectors like :nth-child().
  - Achieve uniqueness by scoping from a stable parent (e.g., page.getByTestId('user-profile').getByRole('button', { name: 'Edit' })).
  - Avoid dynamic, user-specific text (e.g., patient names) as locators for screenshots.
  
  ### Execution Strategy: Maximum Speed & Success Rate
  
  **4. FORCE ALL CLICKS:**
  - All click actions MUST use the { force: true } option. This is non-negotiable to ensure clicks succeed even if elements are partially obscured.
  
  **5. AVOID MANUAL DELAYS AND SLOW TIMEOUTS:**
  - NEVER use page.waitForTimeout(). It is a slow anti-pattern.
  - DO NOT add a timeout option to actions. Rely on the fast default Playwright timeout to report failures quickly.
  - **Rely on Auto-Waiting:** Trust Playwright's built-in auto-waiting for element presence. Your goal is a single, clean action line.
  
  ### Element State Interpretation:
  
  **6. Element State Tags:**
  Elements may include these appended states:
  - **(expanded/collapsed)**: Element visibility state
  - **(controls-expanded-panel/controls-collapsed-panel)**: Button/toggle state for controlled panels
  - **(checked/unchecked/indeterminate)**: Toggle element states
  - **(selected)**: Active item in a group
  - **(current-page)**: Current navigation location
  - **(menu-open/menu-closed)**: Menu/popover/dialog visibility
  - **(focused)**: Keyboard focus state
  - **(required)**: Form field requirement
  - **(invalid)**: Form validation state
  - **(pressed)**: Toggle button activation state
  
  **7. State-Based Action Logic:**
  - If the user script asks you to perform an action that would **toggle** a state (e.g., "expand Menu") and the element is already in the **desired state** (e.g., (expanded)), consider the action complete or verify the state if applicable.
  - If the user script asks you to perform an action to reach a state (e.g., "select Dashboard") and the element is *not* in that state (e.g., the "Dashboard" link is missing (selected) or (current-page)), then plan to click it.
  
  ### Action & Assertion Mapping:
  
  **8. Primary Actions:**
  - **Click**: await locator.click({ force: true });
  - **Type/Enter**: await locator.fill('text');
  - **Verify/Assert**: IMPORTANT: DO NOT use expect() assertions! Instead use these alternatives:
    - Check visibility: await locator.isVisible();
    - Wait for element: await locator.waitFor({ state: 'visible', timeout: 30000 });
  
  **9. Screenshot Requirements:**
  - **IMPORTANT for screenshots:** Do NOT use getByText for screenshot targets. Always select a stable container (test id / role / label) and screenshot that container.
  - When taking a screenshot, you MUST ALWAYS include the "screenshotIntent" field in your JSON response:
    - REQUIRED: Every screenshot command MUST have a corresponding "screenshotIntent" field in your JSON response
    - The screenshotIntent must describe in detail WHAT you are capturing and WHY it's important
    - Example: "screenshotIntent": "Capturing the user profile section to verify all demographic fields are displayed correctly"
    - Always prefer a larger container for the screenshot elements - do not take the exact button screenshot, we need to capture the section those target elements are a part of.
  
  ### Error Recovery & Navigation:
  
  **10. Error Recovery:**
  - If a Previous Step Error exists, the locator was likely wrong. DO NOT repeat the failed code.
  - Your new code must use a more stable locator from the hierarchy. Since clicks are already forced, the primary reason for failure is a bad selector.
  
  **11. Smart Element Discovery:**
  - If you don't find the element mentioned in the instruction to click or perform actions on, then maybe it is hidden behind a dropdown or some general action performed now will help you find it, so maybe find something sensible to click on to find it.
  - If you can't find the properly named buttons, prefer buttons that are next to the element you are trying to click on - maybe it's a menu and you need to click on it to dropdown, then you find the element required.
  - If some elements that are supposed to be visible aren't, then click on related close information - maybe it's collapsed and you need to expand to view information.
  
  **12. Special Navigation Notes:**
  - In document viewer applications, the left panel typically contains documents and information sections as alternatives - click on one of them to access content.
  - If an instruction cannot be performed after reasonable attempts, it's acceptable to skip that specific instruction and move to the next one.
  - If an instruction is impossible (e.g., wait 15 minutes, click on non-existent elements), skip and continue with the next instruction.
  - There might be some elements that are interactive even if they are not selected as buttons so make sure to click on them if that is what is instructed to do.
  
  ### Example Output Structures:
  
  **First Step Example:**
  {
    "thinking": "Current instruction: 'Click the Add button'. As this is the first step there is no thinking of previous step provided. The user's intent is to initiate the add workflow. The visible HTML shows a button with role='button' and name='Add', which is currently enabled. I will click this button to proceed.",
    "code": "await page.getByRole('button', { name: 'Add' }).click({ force: true });"
  }
  
  **Subsequent Step Example:**
  {
    "thinking": "Current instruction: 'Select Dashboard in the sidebar'. Previous instruction: 'Click the Add button'. The 'Dashboard' link in the sidebar is currently *not* tagged as (current-page) in the visible HTML. The user's intent is to navigate to the Dashboard page. I will click on the 'Dashboard' link.",
    "code": "await page.getByRole('link', { name: 'Dashboard' }).click({ force: true });"
  }
  
  **Screenshot Example:**
  {
    "thinking": "Current instruction: 'Take a screenshot of the user profile section'. Previous instruction: 'Navigate to profile page'. The user's intent is to capture the layout of the user profile for verification. The visible HTML provides a data-testid='user-profile-section', which is a stable selector.",
    "screenshotIntent": "Capturing the user profile section to verify the layout of demographic information and settings that will be used for subsequent validation steps.",
    "code": "await page.getByTestId('user-profile-section').screenshot({ path: 'user-profile.png' });"
  }
  
  **State-Based Thinking Examples:**
  {
    "thinking": "Current instruction: 'Expand the Settings menu'. Previous instruction: 'Navigate to the admin panel'. The 'Settings' menu item is currently (collapsed) in the visible HTML. The user's intent is to make it (expanded) to access submenu options. I will click the 'Settings' menu item to expand it.",
    "code": "await page.getByRole('menuitem', { name: 'Settings' }).click({ force: true });"
  }
  
  {
    "thinking": "The current instruction is to click on the 'More Options' menu icon again. Looking at the available interactive elements, element 15, which has data-testid="MoreVertOutlinedIcon" and is a button with a 3-dot icon, perfectly matches the description but it is tagged as (menu-open). I should'nt to click this button as the menu is open already. This instruction is not required as the desired state is already achieved. Moving to the next instruction: 'Click the Save Settings button'.",
    "code": "await page.getByRole('button', { name: 'Save Settings' }).click({ force: true });"
  }
  
  ### Final Execution Guidelines:
  - Make sure the thinking is done before the code is generated.
  - If there are multiple elements named the same as the target, explain the reason why you chose the one you chose.
  - Stick to the instructions as much as possible.
  - Always analyze the current element states before deciding on actions.

-------- Context --------
Page URL: https://team-meta-apim.azure-api.net/document-viewer-v3?patientId=1928&orderId=857&studyId=1018&internalManagingOrganizationID=1&IssuerOfPatientID=RAMSOFT&StudyInstanceUIDs=1.2.124.113540.1.2.13113.17209.16697.13379

Previously Executed Code:
await page.getByText('BLUMEPAT, LNGO MS08').nth(1).click({ force: true, timeout: 20000 });
await page.getByTestId('documentviewer-icon').click({ force: true, timeout: 20000 });
await page.screenshot({ path: 'img_as/documentviewer_S.png' });
await page.locator('div').filter({ has: page.getByTestId('patient-card-title') }).first().screenshot({ path: 'img_as/documentexplorer_E.png' });
await page.getByTestId('patient-card-title').nth(1).screenshot({ path: 'img_as/patientdetails_E.png' });
await page.getByTestId('patient-card-title').getByTestId('PersonIcon').click({ force: true, timeout: 20000 });

Current Step Error Code:
None

Previous Step Thinking:
The current instruction is to locate the pop-out icon ('Abrir detalles del paciente') in the patient details section and click on it. Looking at the visible HTML, element 12 contains the patient's name and a 'PersonIcon' which is likely the pop-out icon. I will use `page.getByTestId('patient-card-title').getByTestId('PersonIcon')` to uniquely identify and click this icon.
